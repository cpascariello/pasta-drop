// Celebration burst configuration — tweak these values to taste

export const CELEBRATION = {
  /** Emoji pool for particles */
  emojis: ['🍝', '🍕', '🧀', '🫒', '🌶️'],
  /** Number of particles to spawn */
  particleCount: 10,
  /** Max distance particles travel from origin (px) */
  spreadRadius: 100,
  /** Total animation duration (ms) */
  duration: 700,
  /** Particle font size range (px) */
  sizeMin: 18,
  sizeMax: 30,
};
